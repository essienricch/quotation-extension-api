require('dotenv').config()
const { DataTypes } = require('sequelize')
const sequelize_db = require('../config/dbconfig')

const Quotation = sequelize_db.define('quotation', {

    name:{
        
    }



})